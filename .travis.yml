language: php

php:
    - 5.6

before_script:
    - wget http://getcomposer.org/composer.phar
    - php composer.phar install --dev --prefer-source
    - mkdir -p web/coverage

script:
    - bin/atoum --test-all

notifications:
    email:
        recipients:
            - ci@verylastroom.com
        on_success: change
        on_failure: change
